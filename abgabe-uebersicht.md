# Abgabe-Übersicht

## Claude-Markierungen im Code

Im Quellcode selbst gibt es **keine** expliziten Claude-Markierungen (keine Kommentare wie "generated by AI" o.ä.).

Stattdessen existieren folgende Meta-Dateien, die auf Claude Code als Werkzeug hinweisen:

| Datei | Inhalt |
|-------|--------|
| `CLAUDE.md` | Projektanweisungen für Claude Code (wie eine `.cursorrules`-Datei) |
| `.claude/settings.local.json` | Claude Code lokale Einstellungen |
| Git-History: Commit `bede21c` | Commit-Message enthält "add .claude/ and build artifacts to .gitignore" |

---

## Dateien, die NICHT zur Abgabe gehören

### Tooling / KI-Hilfsmittel
- `CLAUDE.md` — Anweisungsdatei für Claude Code, nicht Teil des Projekts
- `.claude/` — Claude Code Einstellungen (lokal)
- `abgabe-uebersicht.md` — diese Datei

### Persönliche Notizen
- `karteikarten.md` — Lernkarteikarten, kein Projektcode

### Vorlesungsmaterialien
- `Skript/` — PDFs und Beispiele der Vorlesung (nicht selbst erstellt)

### Credentials / Secrets
- `gruppe-2-kubeconfig.yaml` — Cluster-Zugangsdaten (bereits in `.gitignore`)
- `k8s/spark-secret.yaml` — Spark-Token (Credentials, nicht in Repo)
- `services/analytics-service/spark-server.pem` — TLS-Zertifikat des Spark-Clusters

### Entwicklungshelfer (kein K8s-Deployment)
- `docker-compose.yml` — lokale Entwicklungsumgebung, nicht für Cluster
- `dashboard/index.html` — veraltete lokale Kopie; Inhalt ist in `k8s/dashboard.yaml` eingebettet

### Veraltete Dateien
- `services/analytics-service/api.py` — wurde durch den separaten `analytics-api`-Service ersetzt

---

## Dateien, die zur Abgabe gehören

### Dokumentation
- `README.md`

### Kubernetes-Manifeste (`k8s/`)
- `namespace.yaml`
- `gateway.yaml`
- `kafka-config.yaml`
- `customer-service.yaml`
- `driver-service.yaml`
- `ride-service.yaml`
- `payment-service.yaml`
- `tracking-service.yaml`
- `analytics-cronjob.yaml`
- `dashboard.yaml`

### Services (`services/`)

**customer-service**
- `Dockerfile`, `requirements.txt`
- `app/main.py`, `app/models.py`, `app/database.py`, `app/__init__.py`

**driver-service**
- `Dockerfile`, `requirements.txt`
- `app/main.py`, `app/models.py`, `app/database.py`, `app/kafka_client.py`, `app/__init__.py`

**ride-service**
- `Dockerfile`, `requirements.txt`
- `app/main.py`, `app/models.py`, `app/database.py`, `app/kafka_client.py`, `app/__init__.py`

**payment-service**
- `Dockerfile`, `requirements.txt`
- `app/main.py`, `app/models.py`, `app/database.py`, `app/kafka_client.py`, `app/__init__.py`

**tracking-service**
- `Dockerfile`, `requirements.txt`
- `app/main.py`, `app/models.py`, `app/database.py`, `app/kafka_client.py`, `app/__init__.py`

**analytics-service** (Spark Batch CronJob)
- `Dockerfile`, `requirements.txt`
- `analytics_job.py`

**analytics-api** (Lesezugriff auf Analytics-Ergebnisse)
- `Dockerfile`, `requirements.txt`
- `main.py`

### Sonstiges
- `.gitignore`
